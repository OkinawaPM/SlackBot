#!perl

use File::Spec;
use Cwd 'getcwd';

BEGIN { unshift @INC, File::Spec->catfile(getcwd, "lib") }
require Okinawa::SlackBot;

Okinawa::SlackBot->new(name => $ENV{SLACK_TOKEN}, token => $ENV{BOT_NAME})->run;